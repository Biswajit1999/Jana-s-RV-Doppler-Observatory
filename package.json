{
  "name": "jana-rv-doppler-observatory",
  "version": "4.0.0",
  "private": true,
  "description": "Zero-build radial-velocity exoplanet observatory with real-data ingestion, worker-based period analysis, and auditable RV bundles.",
  "scripts": {
    "audit:observations": "node scripts/audit_rv_library.mjs",
    "check": "node --check app.js && node --check app_backend_console.js && node --check app_column_mapper.js && node --check app_local_rv_library.js && node --check app_rv_sources.js && node --check app_visual_upgrade.js && node --check rvWorker.js && npm run audit:observations"
  }
}
